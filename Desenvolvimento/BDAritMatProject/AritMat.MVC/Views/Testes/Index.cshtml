@model IEnumerable<AritMat.MVC.Models.Teste>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dificuldade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dificuldade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTeste }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTeste }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTeste })
        </td>
    </tr>
}

</table>
